import JdKeywordExtractor from "@/app/components/tools/JdKeywordExtractor";

// Real page, not a redirect — matches /ar/optimize, not /ar/interview.
export default function ArJdKeywordExtractorPage() {
  return <JdKeywordExtractor defaultAr />;
}
